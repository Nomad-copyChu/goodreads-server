input EditAuthorArgs {
  photo: String
  born: String
  died: String
  gernes: [String!]
  description: String
}

type Mutation {
  editAuthor(authorId: ID!, editAuthorArgs: EditAuthorArgs): Author
}
