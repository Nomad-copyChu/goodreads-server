enum Gender {
  Male
  Female
}

type Mutation {
  editProfile(
    profileId: ID!
    username: String
    age: Int
    gender: Gender!
    bio: String
    interests: String
    favoriteBook: String
  ): Profile!
}
