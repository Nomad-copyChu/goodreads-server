type CoronaComment {
  id: ID!
  username: String
  message: String
  editedIpAddress: String
  createdAt: String
  updatedAt: String
}

type Query {
  getComments(cursor: String, limit: Int): [CoronaComment]
}
